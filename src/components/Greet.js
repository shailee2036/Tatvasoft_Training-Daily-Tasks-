import React from 'react'
//functional component (lec-05)
function Greet() {
  return (
    <div>
      {/* <h1>Welcome</h1> */}
    </div>
  )
}
// export const Greet=() => <h1>Welcome</h1> while using this format u have to use destructing when 
 //importing it and u have to use the same name as export else it will throw an eror 
 // this is called named export

export default Greet // while using this default type destructuring is not required




